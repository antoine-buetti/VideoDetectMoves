docker {
    enabled = true
}

wave {
    enabled = true
    freeze = true
    strategy = ['conda']
}

conda {
    enabled = true
    channels = ['conda-forge', 'bioconda', 'defaults']
}

process {
    withName: 'get_frames' {
        conda = 'conda-forge::mplayer conda-forge::procps-ng'
    }
}
